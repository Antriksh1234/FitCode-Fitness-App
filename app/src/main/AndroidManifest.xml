<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitcode_fitterthanyesterday">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BmiHistory" />
        <activity android:name=".ContactDevelopers" />
        <activity android:name=".BodyActivity" />
        <activity android:name=".FinishedActivity" />
        <activity android:name=".RestActivity" />
        <activity android:name=".ExerciseActivity" />
        <activity android:name=".TodayExerciseActivity" />
        <activity android:name=".PlanActivity" />
        <activity android:name=".HomeScreenActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>